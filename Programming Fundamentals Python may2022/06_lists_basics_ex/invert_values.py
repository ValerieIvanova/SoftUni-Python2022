numbers = input().split()
opposite_list = [-int(element) for element in numbers]
print(opposite_list)

# numbers = input().split()
# opposite_list = []
# for num in numbers:
#     opposite_list.append(-int(num))
# print(opposite_list)
